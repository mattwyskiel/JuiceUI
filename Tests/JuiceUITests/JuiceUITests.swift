import XCTest
@testable import JuiceUI

final class JuiceUITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(JuiceUI().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
